{
  "name": "anchor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-pino-logger": "^4.0.0",
    "http-proxy-middleware": "^0.20.0",
    "node-env-run": "^3.0.2",
    "nodemon": "^1.19.2",
    "npm-run-all": "^4.1.5",
    "path": "^0.12.7",
    "pino-colada": "^1.4.5"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "yarn run start && cd client && yarn run start",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  }
}
